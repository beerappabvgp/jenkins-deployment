{
  "name": "backend",
  "module": "index.ts",
  "type": "module",
  "private": true,
  "devDependencies": {
    "@types/bun": "latest"
  },
  "scripts": {
    "generate:db": "cd packages/db && bunx prisma generate",
    "start:backend": "cd apps/backend && bun run index.ts"
  },
  "peerDependencies": {
    "typescript": "^5"
  },
  "dependencies": {
    "@types/express": "^5.0.0",
    "bun": "^1.2.5",
    "db": "*",
    "express": "^4.21.2"
  }
}